server:
    port: 9000
spring:
    datasource:
        username: "${MYSQL_USERNAME}"
        password: "${MYSQL_PASSWORD}"
        url: "jdbc:mysql://localhost:3306/catsandhumans?useSSL=false"
    jpa:
        hibernate:
            ddl-auto: "update"
credentials:
    admin:
        authorities: "ROLE_ADMIN"
        password: "admin"
        username: "admin"
    user:
        authorities: "ROLE_USER"
